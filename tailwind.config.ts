// Tailwind v4 uses CSS-first configuration via @theme in app/globals.css.
// Brand tokens and fonts are defined there. This file is kept for editor tooling only.
import type { Config } from "tailwindcss";

export default {} satisfies Config;
